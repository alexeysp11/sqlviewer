<Window x:Class="SqlViewer.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SqlViewer.Views"
        Title="sqlviewer: Authorization" Height="350" Width="400" 
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize">
    <Grid Margin="30">
        <StackPanel VerticalAlignment="Center" Height="260">
            <TextBlock Text="Login" FontSize="24" FontWeight="Bold" Margin="0,0,0,15" HorizontalAlignment="Center"/>

            <TextBlock Text="Username"/>
            <TextBox x:Name="UsernameBox" Margin="0,5,0,10" Height="25" VerticalContentAlignment="Center"
                     Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}"/>

            <TextBlock Text="Password"/>
            <PasswordBox x:Name="PassBox" Margin="0,5,0,20" Height="25" VerticalContentAlignment="Center"/>

            <Button Content="Login" IsDefault="True" Height="30" Margin="0,0,0,10"
                    Command="{Binding LoginCommand}" 
                    CommandParameter="{Binding ElementName=PassBox}"/>

            <Button Content="Login as a guest" Height="30" Background="Transparent" BorderBrush="Transparent"
                    Command="{Binding GuestLoginCommand}"/>
        </StackPanel>
    </Grid>
</Window>
