<Window x:Class="SqlViewer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SqlViewer"
        xmlns:uc="clr-namespace:SqlViewer.UserControls"
        xmlns:vm="clr-namespace:SqlViewer.ViewModels"
        mc:Ignorable="d"
        WindowStartupLocation="CenterScreen"
        Title="{Binding WindowTitle}"
        MinHeight="525" MinWidth="800"
        Height="600" Width="900">

    <Window.InputBindings>
        <KeyBinding Command="{Binding QuerySqlCommand}" Key="F5" />
    </Window.InputBindings>

    <Window.Resources>
        <HierarchicalDataTemplate DataType="{x:Type vm:TableViewModel}" ItemsSource="{Binding Columns}">
            <StackPanel Orientation="Horizontal">
                <Image Source="TableIcon.png" Width="16" Margin="0,0,5,0"/>
                <TextBlock Text="{Binding Name}" FontWeight="Bold"/>
            </StackPanel>
        </HierarchicalDataTemplate>

        <DataTemplate DataType="{x:Type vm:ColumnViewModel}">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="{Binding Name}" Foreground="Black" FontStyle="Italic"/>
            </StackPanel>
        </DataTemplate>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <uc:Menu Grid.Row="0" />

        <Grid Grid.Row="1" Margin="5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="150" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <ComboBox Grid.Column="0"
                      ItemsSource="{Binding AvailableRdbms}"
                      SelectedItem="{Binding SelectedRdbms}"
                      VerticalContentAlignment="Center"/>

            <TextBlock Text="Connection string:" Grid.Column="1" Margin="10,0,5,0" VerticalAlignment="Center"/>

            <TextBox Grid.Column="2"
                     Text="{Binding ConnectionString, UpdateSourceTrigger=PropertyChanged}"
                     VerticalContentAlignment="Center" />
        </Grid>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="250" MinWidth="150"/>
                <ColumnDefinition Width="5"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <GroupBox Header="Metadata Explorer" Grid.Column="0" Margin="5">
                <TreeView ItemsSource="{Binding DatabaseTables}">
                    <TreeView.ItemContainerStyle>
                        <Style TargetType="TreeViewItem">
                            <Setter Property="IsExpanded" Value="{Binding IsExpanded, Mode=TwoWay}" />
                        </Style>
                    </TreeView.ItemContainerStyle>
                </TreeView>
            </GroupBox>

            <GridSplitter Grid.Column="1" Width="5" HorizontalAlignment="Stretch" Background="#EEE"/>

            <Grid Grid.Column="2">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="5"/>
                    <RowDefinition Height="250"/>
                </Grid.RowDefinitions>
            
                <TextBox Grid.Row="0"
                        AcceptsReturn="True"
                        Margin="5"
                        FontFamily="Consolas"
                        FontSize="14"
                        Text="{Binding SqlQuery, UpdateSourceTrigger=PropertyChanged}"
                        SpellCheck.IsEnabled="False"/>

                <GridSplitter Grid.Row="1" Height="5" HorizontalAlignment="Stretch" Background="#EEE"/>

                <TabControl Grid.Row="2">
                    <TabItem Header="Result">
                        <uc:DbGrid ItemsSource="{Binding QueryResults}"
                            MinHeight="200"
                            MinWidth="635"
                            Height="200"
                            Width="Auto"
                            IsReadOnly="True"
                            AutoGenerateColumns="True"
                            BorderBrush="{DynamicResource {x:Static SystemColors.DesktopBrushKey}}"
                            Background="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}"
                            Grid.IsSharedSizeScope="True" />
                    </TabItem>
                    <TabItem Header="Logs">
                        <TextBox AcceptsReturn="True"
                                Margin="5"
                                Text="{Binding SqlCommandLogs, Mode=OneWay}"
                                IsReadOnly="True"
                                VerticalScrollBarVisibility="Auto"
                                Background="#F9F9F9"/>
                    </TabItem>
                </TabControl>
            </Grid>
        </Grid>
    </Grid>
</Window>
